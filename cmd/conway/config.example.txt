# Example config for the on-device Conway firmware.
# Copy to config.txt (gitignored). It is embedded into the firmware at build
# time. Conway only needs the panel dimensions.

# Display dimensions (pixels)
width: 64
height: 32
